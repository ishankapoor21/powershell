git init :- create (or initialize) a repository 
git add :- add new or updated files to the staging area

[use this 2 commands before git commit]

git config --global user.email "you@example.com"
git config --global user.name "Your Name"

git commit:- to make the commit 
git status
git log
git branch:- list of all the branches 
git checkout -f
git diff
git rm --cached file.extension (remove from staging area)
git status -s
git branch branch_name
git branch
git checkout branch_name (switch from master branch to newly created branch)
git merge branch_name
git branch -b branch_name 
git remote add origin git@github.com:ishankapoor21/ARM-Template.git
git clone

